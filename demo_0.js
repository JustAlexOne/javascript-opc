'use strict';
(function() {

  function registerUser(firstName, lastName) {
    let person = {
      firstName, // using the same name as an arguments, to init person object
      lastName
    }
  }

  display(person);

})();